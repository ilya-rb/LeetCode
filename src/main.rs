#![allow(dead_code)]

mod plus_one;
mod rotate_array;
mod remove_duplicates;
mod contains_duplicate;
mod move_zeroes;
mod two_sum;
mod binary_tree_inorder;

fn main() {
    binary_tree_inorder::test();
}